sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        config:
          freshness:
            warn_after:
              count: 9
              period: day
            error_after:
              count: 12
              period: day
          loaded_at_field: _batched_at